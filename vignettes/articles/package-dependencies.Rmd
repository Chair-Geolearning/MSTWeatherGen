---
title: "package-dependencies"
---
```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```
```{r load-libraries}
library(MSTWeatherGen)
library(pkgnet)
```
```{r create-report, echo=FALSE}
output_dir <- file.path("docs", "articles", "pkgnet")
if (!dir.exists(output_dir)) {
  dir.create(output_dir, recursive = TRUE)
}
report_file <- file.path(output_dir, "pkgnet-report.html")
results <- CreatePackageReport('MSTWeatherGen',report_path = report_file)
```

```
**[Consulter le rapport pkgnet complet](../pkgnet/pkgnet-report.html)**

